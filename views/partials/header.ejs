<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="/public/stylesheets/style.css"></script>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-[100px] ">
        <div class="navbar flex justify-between w-full px-20 py-10">
            <h3 class="font-bold text-3xl" >khaatabook</h3>
            <% var answer = typeof loggedin !== 'undefined'?loggedin : false %> 
            <% if(answer){ %>
                <div class="links flex gap-10">
                    <a class="font-semibold text-xl text-blue-500" href="#">Home</a>
                    <a class="font-semibold text-xl text-blue-500" href="#">Create new hisaab</a>
                    <a class="font-semibold text-xl text-red-700" href="/logout">logout</a>
                </div>
            <% } %>

        </div>
    </div>